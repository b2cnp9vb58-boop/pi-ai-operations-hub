{
  "name": "pi-telegram-claude-control",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "test": "node --test --test-concurrency=1",
    "test:portal": "python -m unittest discover -s tests/portal -v"
  }
}
